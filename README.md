# Interview questions: 

## Getting started: 

To use this project as a reference, you'll need to follow this steps: 

1. Clone this repository.
2. run `npm install`.

# Running the tests:

1. Remove the pre-pended `x` before the test you want to run.
2. Run `npm run test` in the terminal

